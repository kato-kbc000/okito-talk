import { getCurrentUser, searchProfiles, searchPosts } from '../api.js';
const $=id=>document.getElementById(id);const esc=v=>{const d=document.createElement('div');d.textContent=String(v??'');return d.innerHTML};
function renderPeople(rows){$('peopleList').innerHTML='';$('peopleEmpty').style.display=rows.length?'none':'block';rows.forEach(p=>{const el=document.createElement('div');el.className='person-row';el.innerHTML=`<div class="person-avatar">${esc([...(p.display_name||p.username||'沖')][0])}</div><div class="person-info"><strong>${esc(p.display_name||'ユーザー')}</strong><span>@${esc(p.username||'user')}</span><span>${esc(p.bio||'')}</span></div>`;$('peopleList').appendChild(el);});}
function renderPosts(rows){$('postSearchList').innerHTML='';$('postEmpty').style.display=rows.length?'none':'block';rows.forEach(p=>{const el=document.createElement('article');el.className='search-post-card';el.innerHTML=`<strong>${esc(p.profiles?.display_name||'ユーザー')} <small>@${esc(p.profiles?.username||'user')}</small></strong><p>${esc(p.content)}</p><small>${new Date(p.created_at).toLocaleString('ja-JP')}</small>`;$('postSearchList').appendChild(el);});}
async function run(){const q=$('searchInput').value.trim();try{const [people,posts]=await Promise.all([searchProfiles(q),searchPosts(q)]);renderPeople(people);renderPosts(posts);}catch(e){alert(`検索に失敗しました：${e.message}`);}}
$('searchForm').addEventListener('submit',e=>{e.preventDefault();run();});
document.addEventListener('DOMContentLoaded',async()=>{if(!await getCurrentUser()){location.replace('../login.html');return;}run();});
